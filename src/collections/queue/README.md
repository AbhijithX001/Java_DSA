# Queue — Placeholder

Queue examples will be added here.
Add `LinkedList as Queue` or `ArrayDeque` examples when studied.

```java
Queue<Integer> q = new LinkedList<>();
q.offer(1);   // enqueue
q.poll();     // dequeue
q.peek();     // front element
```
