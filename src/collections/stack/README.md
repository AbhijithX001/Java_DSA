# Stack — Placeholder

Stack examples will be added here.

```java
Stack<Integer> stack = new Stack<>();
stack.push(1);   // push
stack.pop();     // pop (removes top)
stack.peek();    // view top
stack.isEmpty(); // check empty
```
